import { createElement } from "../components/elements";

export const statsContainer = (player) => {
  const allContainer = createElement("div", "");
  // 100 / stamina * currentStamina
  const staminaContainer = createElement("div", "");
  alladdChild.addChild(staminaContainer)
  return allContainer;
};
